# Known-risks register (DATAROOM-010)

The standing, honest list of residual risks — seeded from the post-triage
review of the blind second audit (`SPRINT-PLAN-TRIAGE.md` §2–§4) and kept
current as sprints close. One row per risk; when a sprint closes one, flip
the status with the commit hash rather than deleting the row.

**Owners:** `eng` = founder + agents (code) · `counsel` = external legal ·
`auditor` = external SOC 2 / pen test · `iron` = needs reference hardware.

## Critical-path risks

| ID(s) | Risk | Status | Owner | Closes via |
|---|---|---|---|---|
| TENANT-101 / WIRE-001 | Bus-published planes trusted the agent-config `tenant_id` — cross-tenant injection on flow/device/eBPF/endpoint | **CLOSED (Sprint 4)** — consumers verify (tenant, agent) against the registry (fail closed, cached); namespaced lanes are the authoritative tenant; injection tests + fuzz in CI. Residual CLOSED by Sprint 11: agent ids now exist only via cryptographic enrollment (registry rows have issuance provenance), so the forged-pair window requires a stolen live SVID, bounded to 24h + revocable | eng | done |
| WIRE-002 / RED-002 / TENANT-103 / ARCH-004 | No in-repo agent enrollment / SVID issuance — the trust root was operator-manual | **CLOSED (Sprint 11)** — ADR founder-approved (docs/adr/agent-enrollment.md) then implemented: single-use tenant-scoped join tokens (hash-at-rest, atomic consume), repo-managed root→intermediate→leaf CA (root key offline-custody, intermediate sealed via tenantcrypto), 24h CSR-based SVIDs with the SPIFFE tenant claim set by the SERVER from the token, identity-pinned rotation at 2/3 TTL (chain + possession proof + serial provenance), admin API + CLI mint (audited), agent enroll CLI + auto-rotation, every serial recorded (feeds Sprint 12 revocation). Integration tests: happy path, replay, wrong-tenant impossibility, rotation, foreign-CA + bad-proof rejection | eng | done |
| LICENSE-001/002 / DATAROOM-001/002 | `LICENSE` is a TBD placeholder; no EE license text; no CLA/DCO; IP chain unproven for non-founder commits | OPEN — counsel decisions kick off now, commits land last | counsel | Phase L (S32–S33) |
| COMPLY-001 / DATAROOM-006 | No SOC 2 attestation (self-declared mapping only) — caps the headline diligence score regardless of code | OPEN — engage auditor now (6–12 mo) | auditor | S29 evidence pack + external attestation |
| SCALE-002 / DOCS-001 / DOCS-006 / OPS-007 | Numeric scale SLOs, agent overhead, and RTO/RPO are PROVISIONAL — everything is now STAGED (Sprint 17: benchmark set complete incl. path-store write; flow plane in the drive set; M-profile nightly regression gate `scale-gate-m`; live ring-buffer overhead harness `TestLiveOverheadReport`; runbooks in docs/scale-gate.md + docs/agent-overhead.md) — the L/XL + live-overhead RUN on reference hardware is one command each, not executed | OPEN | iron | reference-hardware runs (Sprint 27 completed the DR runbooks + encrypted-restore path + self-hosted-IdP doc; the representative multi-region RTO/RPO drill itself is the residual [needs infra] item) |

## Security residuals (open until their sprint)

| ID(s) | Risk | Status | Owner | Closes via |
|---|---|---|---|---|
| RED-001 / SEC-001 | Dev auth mode was runtime-selectable in every binary | **CLOSED (Sprint 3, c114a05)** — compiled out of release binaries (-tags devauth only), triple-gated (ack + loopback), no-devauth-in-release CI gate | eng | done |
| WIRE-003 / WIRE-004 / WIRE-005 / WIRE-007 | Revocation list never fed; plaintext HTTP fail-open; OTLP/MCP bespoke weak TLS; 1.2 floor fleet-wide | **CLOSED (Sprint 12)** — WIRE-003: `revoke-agent` CLI + audited admin API resolve serials/SPIFFE from the S11 registry, persist (migration 0042), push the live deny-list, boot-reload + 30s refresher; SPIFFE-dimension refuses re-issued certs; enrollment/rotation refuse revoked ids. WIRE-004: non-loopback plaintext REFUSES to start without `PROBECTL_ALLOW_PLAINTEXT_HTTP` (Helm sets it behind its TLS ingress). WIRE-005/007: one hardened config (`crypto.ServerTLSConfig`) for API/OTLP/MCP with a **TLS 1.3 floor** on every probectl↔probectl endpoint (outbound probe clients stay 1.2 for third-party interop, allowlisted + documented); `unified-TLS` lint gate bans bespoke `tls.Config` literals | eng | done |
| SEC-003 | Provider/operator login sat outside the tenant-login rate limiter | **CLOSED (Sprint 9)** — per-account + per-IP throttle with exponential lockout on the provider login (reuses the U-024 `auth.Limiter`), 429 + Retry-After, lockouts audited to the provider stream; tests incl. account-rotation-per-IP | eng | done |
| SEC-004 | OIDC nonce generated and sent but never validated on callback | **CLOSED (Sprint 9)** — nonce stored in a transient cookie at login; the ID token's nonce claim must match on callback (missing cookie or mismatch = 401, fail closed); mismatch + missing-cookie + happy-path tests | eng | done |
| SEC-006 / OPS-003 | Default `probectl` password fallback in non-dev compose; hardcoded creds in the DR overlay | **CLOSED (Sprint 10)** — `${POSTGRES_PASSWORD:?}` required-var syntax: the stack REFUSES to start without an operator-set secret; DR drill reuses the same env ref; `.env.example` ships an empty value with generation instructions | eng | done |
| SEC-007 | SSRF guard blocked only the exact unspecified address, not 0.0.0.0/8 | **CLOSED (Sprint 10)** — full 0.0.0.0/8 denied (the Linux this-network→localhost quirk), incl. v4-mapped smuggles; per-range table tests for every blocked class (loopback, RFC1918, ULA, link-local/metadata, CGNAT, multicast, broadcast) | eng | done |
| RED-003 / EBPF-001/002 | sslsniff TLS capture is host-wide once consented (no per-process allowlist); redaction is post-capture | CLOSED (Sprint 18) — in-kernel allowlist (`scope_tgids`/`scope_cgroups`, empty = match nothing) as the third consent gate (`l7_capture_scope`, host-wide not expressible; exe: re-resolved 10s); kernel capture window (`capture_cfg`, zero default = length-only fail-closed) keeps body bytes out of the ring; `decodeChunk` redacts the only userspace copy; kernel-matrix test proves non-allowlisted → zero events | eng | — |
| WIRE-006 / RED-008 | No app-layer replay/freshness on ingestion; probe `ca_file` read arbitrary agent paths | **CLOSED (Sprint 12)** — results streams carry a timestamp+nonce envelope inside the mTLS channel: stale (±10m) or replayed (per-agent nonce cache, bounded, no eviction-by-flooding) stream opens are refused fail-closed; store-and-forward intact (the window bounds the ENVELOPE, not result timestamps). `ca_file` is contained to the operator-allowlisted dir (`tls.canary_ca_dir`; symlink+traversal-proof; unset = refused) | eng | done |
| ARCH-002 / SEC-005 / TENANT-108 | ClickHouse reached via raw HTTP with string-escaped queries (`chStr`) — injection hardening was manual | **CLOSED (Sprint 7)** — every CH value is a SERVER-BOUND parameter ({name:Type} + param_*, ClickHouse's native HTTP binding); `chStr`/`chTime`/`sqlStr` deleted; DDL identifiers regex-validated (`chValidUser`), fail closed; injection tests (unit + real-CH) + the `no-stringbuilt-sql` lint gate. FOUNDER DECISION: binding over the existing HTTP transport instead of adopting clickhouse-go/v2 — same security property, no new dependency tree, §4 architecture (breaker/silo-router/TLS) intact | eng | done |
| SEC-002 / COMPLY-004 | Keyless plaintext passthrough was the compose default; bulk-store encryption duty undocumented | **CLOSED (Sprint 8)** — at-rest sealing ON by default (generated-or-required key file on the `controldata` volume + `REQUIRE_AT_REST_ENCRYPTION=true` in compose/Helm, fail-closed); operator duty documented (`docs/hardening.md` §0c) with `probectl-control preflight --strict` detecting unencrypted backing mounts (+ attestation env for cloud volumes) | eng | done |
| SEC-008 / SEC-009 / RED-007 | SCIM errors echoed internals; /openapi.json + /version open; cookie Secure tied to the app listener; MCP stdio auth unverified | **CLOSED (Sprint 9/10)** — SCIM store errors generic (details server-side); /openapi.json auth-gated + /version detail-stripped outside dev; `PROBECTL_PUBLIC_TLS` drives Secure at the deployment edge (Helm sets it); RED-007 VERIFIED: mcp-stdio authenticates before serving, pinned by test, local-trust model in docs/mcp.md | eng | done |
| EBPF-003 / EBPF-004 / EBPF-005 / EBPF-006 / EBPF-007 / EBPF-008 | Object integrity was a self-checksum (no signature chain decision); shipped image was the FIXTURE build; capability probe omitted CAP_PERFMON (ready-then-fail-at-attach); denylist gap; systemd unit lacked resource caps; kernel matrix x86-only | **CLOSED (Sprint 19)** — EBPF-003 DECIDED per triage rule: operator-supplied objects NOT supported; embedded-digest chain documented as the trust boundary (source→bpf2go→embed→U-014 manifest→cosign-signed binary covers objects+manifest together) with a static tripwire (`TestNoOperatorSuppliedBPFObjectPath`) forcing the signature design if a load path ever appears. EBPF-004: `Dockerfile.ebpf` ships the LIVE `-tags ebpf` build (release.yml wired); `ebpf-image-live` ci job fails unless the image binary records the tag; fixture documented dev/test-only. EBPF-005: probe now checks CAP_PERFMON (attach) separately from CAP_BPF (load), SYS_ADMIN implies both, distinct actionable reasons; CAP_NET_ADMIN documented NOT required (no TC/XDP). EBPF-006: `bpf_probe_write_user` confirmed in the forbidden list with the Sprint 0 re-audit note. EBPF-007: unit gains CPUQuota=100%/MemoryHigh=384M/MemoryMax=512M/TasksMax=128/IOWeight mirroring Helm limits. EBPF-008: matrix adds arm64 (native runner) + hardened lockdown=integrity entry (one-way raise inside the ephemeral VM; probe truthfulness + load/attach asserted); secure-boot distro-kernel image remains the [needs infra] extension if the ci-kernel lacks the lockdown LSM | eng | — |
| AIRCA-001 / AIRCA-002 / AIRCA-003 / AIRCA-005 / RED-005 | MCP egressed raw tenant telemetry without main-path controls; redaction IP-focused; MCP reads unaudited; authoring bypassed consent; uncited root_cause passed on one path | **CLOSED (Sprint 20)** — ONE `ai.EgressGate` (consent + redaction + audit), constructed once, gates ALL external-AI egress: remote RCA model, MCP tool results (gate is a REQUIRED `mcp.New` argument — gate-less server not constructible; results redacted text+structured; same `ai_remote_egress` consent, default deny), and authoring (`GatedCompleter`, principal-derived tenant, fail closed). Redactor extended (not rewritten): emails/phones/MACs (`PROBECTL_AI_REDACT_PII`, default on) + operator custom patterns (`;;`-separated, compile-checked at boot, fail closed); JSON-safe secret masking. Every MCP call audited (`mcp.tool_call`: actor/tool/outcome incl. consent+permission+rate denials). RED-005: `Synthesis.RootCauseCitations` validated like findings on EVERY adapter path — an uncited root_cause is REJECTED (replaced + flagged ungrounded + confidence forced low); injection test proves the old one-grounded-finding bypass is closed. Static gate `TestNoAIClientOutsideGate` bans AI clients outside the adapter | eng | — |
| AIRCA-004 / TEST-003 | No cache/breaker on the remote-model path (a slow provider degraded RCA); RCA quality eval non-blocking | **CLOSED (Sprint 21)** — `ResilientModel` wraps the configured-model path: `internal/breaker` (3 fails → 30s open), ctx-enforced model timeout, content-keyed response cache (10m/256; citations remapped positionally onto session-random IDs so grounding still validates), and graceful degradation to the air-gapped builtin with a PARTIAL RESULT banner + `degraded` flag (grounded citations — RED-005 holds while degraded); authoring Complete shares the breaker. TEST-003 per FOUNDER DECISION: `rca-eval` is BLOCKING (continue-on-error removed — zero occurrences repo-wide) with floors answer_accuracy ≥ 0.85 AND citation_precision ≥ 0.85 (run-verified 0.913/0.924 against the 0.91/0.92 baseline); report artifact uploads on failure too; analyzer pytest already gates (test-python) | eng | — |
| ARCH-001 / ARCH-006 | OTLP receiver was metrics-only (traces/logs absent); no OTel Collector path — custom pipeline limited ecosystem reuse | **CLOSED (Sprint 22)** — the receiver speaks ALL THREE OTLP signals (gRPC Trace/Logs/Metrics services + HTTP /v1/traces,/v1/logs,/v1/metrics), same contract per signal: TLS-only, token→tenant auth, server-side tenant verify/stamp (mismatch = 403), bounded input. Per-signal bus topics → consumers → stores: metrics→TSDB (S16), traces+logs→otelstore (memory + ClickHouse `(tenant_id, day)` partition, chmigrate v1, bound-param queries, retention TTL default 30d, EraseTenant). Queryable at GET /v1/otlp/traces|logs (tenant-first, metrics.read; OpenAPI'd). Three-signal round-trip + cross-tenant rejection pinned in CI (TestOTLPThreeSignalRoundTrip). ARCH-006: probectl IS a standard OTLP endpoint — stock Collector exports via otlphttp (reference config deploy/otel-collector/config.yaml, runbook docs/otlp.md); live Collector exercise = [needs infra]. Claims wording updated to actual coverage (docs/otlp.md authoritative; README/architecture aligned; DOCS-005/S30 coordinated) | eng | — |
| SUPPLY-001 / SUPPLY-002 / SUPPLY-003 / SUPPLY-006 | Compose defaulted to :latest; ruff/black + pyyaml unpinned in CI; analyzer had no lockfile (>= floors) | **CLOSED (Sprint 23)** — compose defaults pin the release tag (v0.4.0; dependabot docker ecosystem bumps; digest-pin guidance for operators — a registry-resolved @sha256 default lands with dependabot's first authenticated pass); ruff==0.15.16/black==26.5.1/pyyaml==6.0.3 exact in CI; analyzer/requirements-dev.lock is hash-locked (uv pip compile --generate-hashes, 219 hashes) with --require-hashes installs + a lock↔pyproject drift check (uv itself pinned); dependabot pip ecosystem added; `supply-pins` CI gate (SELFTEST'd) fails on :latest under deploy/, unpinned go install, or unpinned pip install; pinning policy in docs/dependency-policy.md. SUPPLY-004 was already closed (vimto@v0.4.0); SUPPLY-005 struck at triage — courtesy provenance note in docs/build/toolchain.md (official go1.26.x, sumdb-verified, toolchain-directive-pinned) | eng | — |
| STATIC-ONLY (methodology) | Diligence verification was static-only: no executed build/test/scan/load receipts under one gate | **CLOSED (Sprint 25)** — `make verify-all` umbrella (scripts/verify_all.sh): build + lint + `-race` tests + repo guards + govulncheck + trivy + eBPF object compile, every output tee'd to receipts/, missing tools FAIL (no silent skips). CI `verify-all` job is the single required check needing ALL 29 verification gates (incl. kernel-matrix eBPF load+attach on real kernels, blocking govulncheck/trivy — exit-code 1, zero continue-on-error repo-wide) and archives the gate→result map (verify-all-receipt, 90d) alongside the per-gate receipts (DATAROOM-005/-011). Surfaced+fixed in the run: go.mod `go` directive bumped 1.26→1.26.4 — bare minor made govulncheck attribute stdlib as 1.26.0 and false-flag 17 already-patched CVEs; govulncheck now exit=0 (executed locally, sumdb-verified toolchain download exercised) | eng | — |
| OPS-008 / DOCS-007 (IdP + runbooks) | Air-gap needed an external OIDC IdP; DR runbooks incomplete | **CLOSED (Sprint 27)** — OPS-008: docs/auth/self-hosted-idp.md documents the air-gapped self-hosted OIDC path (Dex in-cluster reference config + Keycloak), with the relying-party contract probectl already satisfies (discovery/nonce/TLS-validated); the air-gap stand-up is the [needs infra] exercise. DOCS-007: region-failover + backup-restore runbooks completed (encrypted-backup decrypt step added). The representative multi-region RTO/RPO drill (OPS-007/DATAROOM-008) remains [needs infra] — tracked in the SCALE-002/OPS-007 row, owner iron; PROVISIONAL banners stay until a representative run, never fabricated | eng | — |
| OPS-001 / OPS-004 / OPS-005 / OPS-009 | Agent DaemonSet had no probes; NetworkPolicy shipped allow-all holes; no /metrics scrape endpoint; backup CronJobs not chart-managed (OPS-006 struck — migrations run at boot, U-046) | **CLOSED (Sprint 26)** — OPS-001: eBPF agent gains a loopback health server (/healthz=process, /readyz=flow-source attached) wired to DaemonSet liveness/readiness probes (control Deployment already had them). OPS-004: deploy/helm/probectl/values-strict.yaml is full default-deny — named ingress-controller + monitoring selectors (HOLE 1 closed), explicit datastore/bus/IdP egress allow-list (HOLE 2 closed), no allow-all rule survives; the U-086 default keeps its documented holes by design; referenced from docs/hardening.md §3a. OPS-005: dependency-free internal/metrics exposes Prometheus self-metrics (build/uptime/goroutines/heap + extensible counters — process/aggregate only, no tenant data) at GET /metrics (pre-auth like /healthz) + a gated ServiceMonitor. OPS-009: PG+CH backup CronJobs folded into the chart behind backup.enabled (off by default; optional chart-created PVC). helm-gate asserts probes + strict default-deny + ServiceMonitor/CronJob gating; kubeconform validates the strict render; helm lint clean | eng | — |
| OPS-010 | CI talked plaintext to Postgres (sslmode=disable fixtures) | **CLOSED (Sprint 10)** — every DB-backed CI job starts Postgres with TLS under a per-run test CA and connects `sslmode=verify-full` (`scripts/ci_pg_tls.sh`); local-dev fallbacks documented in docs/development.md | eng | done |
| TENANT-105 / TENANT-107 | Cross-tenant suite predated Sprint 4/5 — no ingest-path injection or query-scoping coverage; siloed-namespace routing untested | **CLOSED (Sprint 6)** — end-to-end ingest injection cases (flow→real CH; device + eBPF/endpoint via the RLS-scoped registry binding on real PG; OTLP token→sink), a predicate-free CH reader proving the row policy (not the app WHERE) scopes reads, and siloed records routing ONLY to namespaced topics + RED-006 fail-closed construction. Runs in the existing required `cross-tenant-isolation` job (no new plumbing) | eng | done |
| TENANT-102 | CH service account was policy-exempt — app compromise = cross-tenant read | **CLOSED (Sprint 5)** — opt-in per-request setting scoping + reader row policy (fail-closed when unset); split reader/writer documented (`docs/security/tenant-isolation.md`). Residual by design: the write/service account stays read-capable for ingest+migrations until the reader split is enabled | eng | done (operator-enabled) |
| CODE-002 | Ignored `json.Unmarshal` errors on ABAC/audit/store reads (`store/abac.go:27,30` is fail-open-ish under deny-override) | OPEN | eng | Sprint 2 (next) |
| CODE-006 | Committed test private key | **CLOSED (Sprint 1, 425143b)** — key deleted, runtime keygen via internal/crypto, gitleaks secret-scan gate | eng | done |
| SCALE-001 / SCALE-008 / SCALE-009 / SCALE-012 / SCALE-013 | Single-threaded per-topic consume with inline remote-write; device-plane drops on store failure; per-path inserts; per-heartbeat UPDATEs; 6× fan-out re-decode | **CLOSED (Sprint 14)** — key-sharded parallel poll dispatch (PROBECTL_BUS_WORKERS, per-key FIFO, commit-after-batch) + a bounded write stage decoupling decode from remote-write (backpressure, retry+DLQ preserved); the device plane now rides the same retry+DLQ contract (original bytes, drop only when the DLQ itself is down — tested); decode-once ResultFan feeds six sidecar sinks from ONE subscription (bench: ~5.8× less decode work); heartbeats coalesce to one multi-row UPDATE per tenant per 2s window; path saves batch cross-path (N discoveries → 2 inserts). BenchmarkIngest* suite runs nightly (ingest-bench artifact). Follow-up: SLO/compliance/BGP sidecars can join the fan incrementally | eng | done |
| SCALE-003 / SCALE-004 / SCALE-005 / SCALE-007 / SCALE-011 | Limiters never evicted; rate limits OFF by default; device plane uncapped; tenant-key hot partitions; sync enrichment on the hot path | **CLOSED (Sprint 15)** — cardinality identities idle past 1h EVICT (amortized sweep, live series refresh, empty tenants removed; Evicted/ActiveSeries in stats; memory-bound test). Bounded-by-DEFAULT fairness on every plane (results 1000/s, flow 10k/s, bytes 2MiB/s, device 2000/s per tenant; NEGATIVE = explicit unlimited; throttle test). Device plane gains the gate + its own cardinality cap (verify-first confirmed it had neither). Bus keys are tenant|bucket with AGENT entropy — a large tenant spreads across ≤16 partitions while per-agent FIFO holds (trade-off documented at bus.TenantKey). Flow enrichment is async: cache hits inline (87ns), misses warm in background and the record proceeds unenriched (1.36ms sync lookup off the hot path; shed warms counted). Cross-replica limiter sharing DELIBERATELY skipped: per-replica caps tolerate replicas×cap worst case instead of a stateful hot-path dependency | eng | done |
| SCALE-006 / SCALE-010 / SCALE-014 / ARCH-005 | Path tables unbounded (no TTL); OTLP topic had NO consumer (silent drop); O(n) memory-TSDB query; silences/acks lost on restart | **CLOSED (Sprint 16)** — path tables v2: (tenant_id, day) partitioning + boot-applied retention TTL (default 90d; PARTITION BY is immutable so v2 recreates the tables — pre-GA, path snapshots are re-discoverable, discard recorded). OTLP metrics CONSUMER drains the topic into the TSDB (gauge+sum tenant-labeled like every plane, bounded labels; histograms counted+skipped until Sprint 22) with a push→query round-trip test. Memory TSDB queries ride a per-metric index that survives eviction + erasure (bench: one metric of 200 in ~4.4µs). ARCH-005 per the standing ADR: ONLY silences/acks persist (alert_ops, RLS-forced, migration 0043) — restored ops re-apply when the series fires post-restart, expired silences skipped, resolve deletes the row; engine-level restart-survival test + the ADR stands for topology/detections | eng | done |
| OPS-002 / COMPLY-002 | Backup artifacts unencrypted; tenant erasure attestation does not cover backups | **CLOSED (Sprint 27)** — OPS-002: internal/backup streaming envelope encryption (fresh DEK/backup wrapped by the Sprint-8 KEK; chunked AES-256-GCM with chunk-index AAD = truncation/reorder evident) + `probectl-control backup-seal`/`backup-open` stdin/stdout filters; the chart's PG backup CronJob pipes the dump through backup-seal (init-staged binary + key secret), so plaintext never hits the volume — round-trip + plaintext-never-on-disk + tamper/truncation tests pass, CLI exercised end-to-end. ClickHouse server-side BACKUP encrypts via the §0c volume duty (documented). COMPLY-002: the erasure attestation now quantifies a BOUNDED backup-coverage window — BackupErasureDeadline = erased_at + PROBECTL_BACKUP_RETENTION_DAYS, bound into the tamper-evident report hash (test proves the deadline is hashed + honest note-only fallback when unquantified) | eng | done |
| COMPLY-003 | Residency is a reporting label for pooled tenants (honest per U-042); siloed/hybrid enforcement not yet implemented | OPEN | eng | Sprint 28 (scoped: enforce for siloed/hybrid, disclose for pooled) |

## Accepted risks (deliberate, ADR/decision-backed — re-justify, don't re-litigate)

| Decision | Rationale | Reference |
|---|---|---|
| StreamConfig RPC stays in the schema (explicit deny, no client stub) | Wire compatibility; removal = buf-breaking for zero capability gain; agent has no config-apply path. **Sprint 13 (ARCH-003) enforced it**: the server answers immediate `Unimplemented` citing the ADR (no frame, no held stream — deny test fails the build if a frame returns); a static test asserts the agent has no client invocation; zero proto change | `docs/adr/config-push.md` (U-044) + Sprint 13 hardening (done) |
| NetworkPolicy default keeps two documented holes; strict profile available | Default-deny `ingressFrom` locks out unknown ingress controllers; holes are loud + documented | U-086 + `values-strict.yaml` (Sprint 26) |
| Topology/detections rebuild on restart (no persistence) | They are caches of durable streams; cold-start tests prove rebuild; only silences/acks (operator inputs) get persisted | `docs/adr/volatile-stores.md` (U-047) + Sprint 16 |
| Isolation tests stay behind tags/skip-harness | They require live PG/CH; the CI job is required — coverage is enforced where it can run | Sprint 6 triage decision |
| cilium/ebpf pre-1.0 on the privileged path | De-facto standard; verifier is the safety boundary; kernel-matrix CI on every bump | `docs/dependency-policy.md` (U-081) |
| MCP stdio trusts the local invoking process (token verified at the binary entry) | Local-process transport; tenant + RBAC still enforced per call | verify + test in Sprint 9 |

## Structural risks (no sprint closes these)

| Risk | Mitigation |
|---|---|
| Solo founder bus factor | Verification net (~30 CI gates), documentation density, ADRs; not eliminated |
| Adoption metrics untested until public launch | PRD v1.0 §8 separates readiness (measurable now) from adoption (clock starts at launch) |
| Open-data/threat-intel AUPs for commercial resale | Tracked per source (`docs/opendata-aup.md`); counsel item before MSP channel opens |
