apiVersion: v1
kind: Service
metadata:
  name: {{ include "netctl.fullname" . }}
  labels:
    {{- include "netctl.labels" . | nindent 4 }}
spec:
  # ClusterIP by default: the API is reachable externally ONLY through the
  # TLS-terminating ingress (no plaintext NodePort/LoadBalancer).
  type: {{ .Values.service.type }}
  selector:
    {{- include "netctl.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
