{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "netctl.serviceAccountName" . }}
  labels:
    {{- include "netctl.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
