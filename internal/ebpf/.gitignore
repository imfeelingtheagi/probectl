# Generated by bpftool + bpf2go at `make ebpf-agent` on a toolchain machine
# (clang + BTF). These are build artifacts of the -tags ebpf build and are not
# committed — the default build ships the stub source and needs none of them.
bpf/vmlinux.h
*_bpfel.go
*_bpfeb.go
*_bpfel.o
*_bpfeb.o
